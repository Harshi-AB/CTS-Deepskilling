// Post.js
// A simple model class that represents a single blog post.
// Matches Figure 2 in the hands-on lab exactly.
class Post {
  constructor(id, title, body) {
    this.id = id;
    this.title = title;
    this.body = body;
  }
}

export default Post;
