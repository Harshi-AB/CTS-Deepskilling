<?xml version="1.0" encoding="UTF-8"?>
<!--
    persistence.xml
    ----------------
    JPA configuration file. Hibernate is used as the JPA provider.
    This file MUST live at META-INF/persistence.xml relative to a
    classpath root (that's why it sits under the project's META-INF folder
    and "." is included on the runtime classpath).

    Update the jdbc url / user / password below to match your local
    MySQL installation before running any exercise.
-->
<persistence xmlns="http://xmlns.jcp.org/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://xmlns.jcp.org/xml/ns/persistence
             http://xmlns.jcp.org/xml/ns/persistence/persistence_2_2.xsd"
             version="2.2">

    <persistence-unit name="ormlearn-unit" transaction-type="RESOURCE_LOCAL">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <class>Employee</class>
        <class>Department</class>
        <class>Skill</class>

        <exclude-unlisted-classes>true</exclude-unlisted-classes>

        <properties>
            <!-- MySQL connection settings -->
            <property name="javax.persistence.jdbc.driver" value="com.mysql.cj.jdbc.Driver"/>
            <property name="javax.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/ormlearn?useSSL=false&amp;serverTimezone=UTC&amp;allowPublicKeyRetrieval=true"/>
            <property name="javax.persistence.jdbc.user" value="root"/>
            <property name="javax.persistence.jdbc.password" value="root"/>

            <!-- Hibernate settings -->
            <property name="hibernate.dialect" value="org.hibernate.dialect.MySQL8Dialect"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <!-- Schema is created manually via sql/schema.sql, so Hibernate must not alter it -->
            <property name="hibernate.hbm2ddl.auto" value="none"/>
        </properties>
    </persistence-unit>
</persistence>
